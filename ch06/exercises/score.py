class Score:
  def __init__(self):
    self.defaultscore = 0 # default score set to 0
    self.scoreup = self.defaultscore + 100 # score increases depends
    if self.collect = True:
      self.scoreup
    else self.collect = False
    break
    